{
  "name": "gq1-playwright-test",
  "version": "1.0.0",
  "description": "Playwright automation for GQ1 login/logout",
  "scripts": {
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
